import { Story, Meta } from '@storybook/react'
import Typography from '.'

export default {
    title: 'Typography',
    component: Typography,
} as Meta

export const Default: Story = () => <Typography />
